const MainButtonGrey = ({ label }: { label: string }) => {
  return (
    <button className="main-button-grey section__secondary-text">
      {label}
    </button>
  );
};

export default MainButtonGrey;
